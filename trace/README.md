# trace

Reserved for span/trace tooling (call graphs, distributed traces, export).

Not populated yet. Host metrics and alerts live under `otel/`;
microbenchmarks and flame-style profiles stay in `bench/`.
