# Changelog

## 0.6.1

- Stable fingerprints (type + frames; volatile ids stripped from messages)
- Concrete `capture_error` type names; public `MemoryTransport` and `Guard::flush`

## 0.6.0

- Initial family-aligned 0.6.0 release
